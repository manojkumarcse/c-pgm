#include<stdio.h>
#include<conio.h>
void main()
{
char str[50];
clrscr();
{
int i=0,n;
scanf("%[^\n]s",str);
n=strlen(str);
for(i=0;str[i]!='\0';i++)
{
if(i==0)
str[i]=(str[i]);
if(str[i]==)
str[i+1]=(str[i+1]);
}
return 0;
}
getch();
}
