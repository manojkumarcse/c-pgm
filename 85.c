#include<stdio.h>
#include<conio.h>
void main()
{
char str[20];
int i,n;
printf("\nenter the string:\n");
scanf("%s",str);
n=strlen(str);
for(i=0;i<n;i++)
{
printf(str.i)
else
}
return 0;
}
getch();


