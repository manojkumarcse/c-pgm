#include<stdio.h>
#include<conio.h>
void main()
{
char str[50],temp[50];
int i,k,j;
printf("\nenter the string:");
scanf("%[^\n]s",str);
for(i=0;str[i]!='\0';i++)
{
if([str]=i)
}
temp =k;
str[i]=str[j];
str[j]=str[k];
}
return 0;
getch();
